//Tabla de notas que existe en el servidor

CREATE TABLE notas (id INTEGER PRIMARY KEY, id_usuario INTEGER, title VARCHAR(100), content TEXT, creationDate VARCHAR(25), modificationDate VARCHAR(25),orderModificationDate VARCHAR(20), deleted CHAR(1), uploaded CHAR(1));
ALTER TABLE `usuarios` CHANGE `id_usuario` `id_usuario` INT(11) NOT NULL;
//Tabla de notas que existe localmente (solo difieren por el id_usuario)
CREATE TABLE notas2 (noteId INTEGER PRIMARY KEY, title VARCHAR(100), content TEXT, creationDate VARCHAR(25), modificationDate VARCHAR(25),orderModificationDate VARCHAR(20), deleted CHAR(1), uploaded CHAR(1));
/***

Metodos para rehacer una tabla y quitar indices en SQLite sin perder data


INSERT INTO notas2 (noteId, title, content, creationDate, modificationDate,orderModificationDate, deleted) SELECT id, title, content, creationDate, modificationDate,orderModificationDate, deleted FROM notas;

ALTER TABLE notas RENAME TO notas_old; (Aqui se podria hacer un DROP)
ALTER TABLE notas2 RENAME TO notas; 
*****/
INSERT INTO notas VALUES(0,'Nota Fantasma', 'Phantom Note', '1975-01-01', '1975-01-01', '0000000000', 'N','N')

//Consulta para obtener data de usuario
SELECT * FROM usuarios WHERE password=SHA2('zeusensacion',256);
//Consulta para insertar usuarios
INSERT INTO usuarios VALUES (null, 'zeus@gmail.com', 'Zeus el Dios', SHA2('zeusensacion',256));

