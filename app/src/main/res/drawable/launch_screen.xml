<?xml version="1.0" encoding="utf-8"?>
<!-- The tag layer-list is a way to paint all the layers defined by every item in the same order as the items are declared, in this example first is painted a background of dark color and then is painted the app logo-->

<!-- The android:opacity=”opaque” line — this is critical in preventing a flash of black as your theme transitions. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" android:opacity="opaque">

    <!-- The background color, preferably the same as your normal theme -->
    <item android:drawable="@color/background_dark"/>

    <!-- Product logo -->
    <item>

        <bitmap
            android:src="@drawable/app_logo"
            android:gravity="center"/>

    </item>

</layer-list>